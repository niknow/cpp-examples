/* This is a comment and will be ignored by the compiler.*/

/* Comments can be used to explain what happens and can
have multiple lines.*/
/* (C++ distinguishes between single and multi line comments.) */

#include <stdio.h> /*This includes the standard input/output library header*/

/* The main function is the entry point into the progrm. */
/* The arguments argc and argv are used to handle command line arguments
discussed later.*/
int main(int argc, char* argv[])
{
    printf("Hello World!\n"); /* This prints 'Hello World!' (the \n ends the line) */
    return 0; /* The main program should exit by returning 0. */
}
